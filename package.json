{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/Trello.postman_collection.json -e tests/Trello_API.postman_environment.json -- reporters junit --reporter-junit-export newman.xml"
  },
  "author": "David",
  "dependencies": {
    "newman": "^3.9.3"
  }
}
